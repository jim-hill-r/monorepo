# Priority Issues

- TODO: Build a release version of cast for linux architecture and commit it to artifacts
- TODO: Rename `cast toolchain install` command with simply `cast install`.
- TODO: Update `cast install` command to always install default tooling even if a cast configuration is not found.
- TODO: Add a `rustup` tool which is a default tool to `cast install` that gets installed via `curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y`

# Backlog

