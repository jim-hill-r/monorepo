# Priority Issues

- TODO: When running `cast deploy` from a project, it should also deploy any deploy projects listed in the deploys field of the cast configuration. For example, running `cast deploy` from `cookbook/web` should deploy `cookbook/cloudflare`.

# Backlog

- TODO: Add description to `cast session` command
- TOOD: Add description to `cast project` command
