# Priority Issues

- TODO: Use `content/intro.md` on the `cookbook/web` landing page as a description of the website. Do not duplicate the content of intro.md, but rather import it into `cookbook/web` so that updates to the `.md` will be represented in subsequent builds.