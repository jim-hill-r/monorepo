
# Priority Issues

- TODO: Stop trying to run CodeQL workflow for rust. It never finishes. Either remove it or make it so that it only runs against projects that have changed.
- TODO: Add a github workflow called `nightly` that runs `cast ci --recursive 2 --check`. It should run on a cron every night at 2 am pacific time.

# Backlog

# Priority Projects
- cast
- cookbook

# On Hold Projects
- luggage
- starcraft