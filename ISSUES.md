
# Priority Issues

- TODO: Running `cast run` within the `cookbook/web` project directory should result in `dx` serving the app. Currently, it tries to run `cargo run` but for framework `dixous` it should use `dx serve`.

# Backlog

# Priority Projects
- cast
- cookbook
