[package]
name = "my_lang"
version = "0.1.0"
edition = "2024"

[dependencies]
# LLVM bindings for code generation
# Using LLVM 18 which is widely available on Ubuntu 22.04+ and macOS
inkwell = { version = "0.8.0", features = ["llvm18-1"] }
