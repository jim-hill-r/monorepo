[package]
name = "standards"
version = "0.1.0"
edition = "2021"

[lints.clippy]
unwrap_used = "warn"
expect_used = "warn"

[lints.rust]
unsafe_code = "forbid"

[dependencies]
clap = { version = "4", features = ["derive"] }

[[bin]]
name = "standards"
path = "src/bin/standards.rs"

[dev-dependencies]

[features]
default = []
