[workspace]
resolver = "2"
members = ["cloudflare", "core", "desktop", "mobile", "ui", "web"]
default-members = ["core", "ui"]

[workspace.lints.clippy]
unwrap_used = "warn"
expect_used = "warn"

[workspace.lints.rust]
unsafe_code = "forbid"

[workspace.dependencies]
dioxus = { version = "0.7.4" }
thiserror = "2.0.18"
tracing = "0.1.44"
auth_sdk = { path = "../auth_sdk" }

# workspace
core = { path = "core" }
ui = { path = "ui" }

[profile]

[profile.wasm-dev]
inherits = "dev"
opt-level = 1

[profile.server-dev]
inherits = "dev"

[profile.android-dev]
inherits = "dev"
